@using Microsoft.AspNetCore.Identity
@inject UserManager<ApplicationUser> UserManager;
@if (!User.Identity.IsAuthenticated)
{
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" href="@Url.Action("Login", "Account")"><i class="bi bi-box-arrow-in-right"></i> Log in</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="@Url.Action("Register", "Account")"><i class="bi bi-person-plus"></i> Register</a>
        </li>
    </ul>
}
else
{

    <div class="dropdown">
        <a class="btn btn-secondary dropdown-toggle dropdown-btn" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="bi bi-person-badge"></i> @UserManager.GetUserName(User)
        </a>
        <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="@Url.Action("UserClaim", "Account")"><i class="bi bi-person"></i> Account</a></li>
            <li><a class="dropdown-item" href="@Url.Action("Index", "Order")"><i class="bi bi-list"></i> Orders</a></li>
            <li>
                @if (User.IsInRole("Admin"))
                {
                    <a class="dropdown-item" href="@Url.Action("Users", "Account")"><i class="bi bi-people"></i> Users</a>
                }
            </li>
            <li>
                <form asp-controller="Account" asp-action="Logout" class="dropdown-item">
                    <button type="submit" class="btn btn-primary-outline"><i class="bi bi-box-arrow-in-right"></i> Log out</button>
                </form>
            </li>
        </ul>
    </div>
    @*<ul class="navbar-nav">
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <i class="bi bi-person-badge"></i> @UserManager.GetUserName(User)
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
                <a class="dropdown-item" href="@Url.Action("UserClaim", "Account")">Account</a>
                <a class="dropdown-item" href="@Url.Action("Index", "Orders")"><i class="bi bi-list"></i> Orders</a>
                @if (User.IsInRole("Admin"))
                {
                    <a class="dropdown-item" href="@Url.Action("Users", "Account")"><i class="bi bi-people"></i> Users</a>
                }
                <form asp-controller="Account" asp-action="Logout" class="form-inline">
                    <button type="submit" class="btn btn-outline-danger my-2 my-sm-0"><i class="bi bi-box-arrow-in-right"></i> Log out</button>
                </form>
            </div>
        </li>
    </ul>*@
}
    @*  <div>
        <i class="bi bi-person-bagde"></i>@UserManager.GetUserName(User)
        <a class="" asp-controller="Account" asp-action="UserClaim">Account</a>
        <a class="" asp-controller="Orders" asp-action="Index">
            <i class="bi bi-list"></i> Orders
        </a>
        @if (User.IsInRole("Admin"))
        {
            <a class="dropdown-item" asp-controller="Account" asp-action="Users">
                <i class="bi bi-people"></i> Users
            </a>
        }
    </div>*@
    @* <div class="dropdown">
        <a class="btn btn-outline-success dropdown-toggle" role="button" id="profile-btn" data-bs-toggle="dropdown"
       aria-expanded="false">
            <i class="bi bi-person-badge"></i> Hello @UserManager.GetUserName(User)
        </a>
        <div class="dropdown-menu" aria-labelledby="profile-btn">
            <a class="dropdown-item" asp-controller="Account" asp-action="Setting">Account</a>

            <a class="dropdown-item" asp-controller="Orders" asp-action="Index">
                <i class="bi bi-list"></i> Orders
            </a>

            @if (User.IsInRole("Admin"))
            {
                <a class="dropdown-item" asp-controller="Account" asp-action="Users">
                    <i class="bi bi-people"></i> Users
                </a>
            }
        </div>
    </div>*@
    @* <span style="padding: 5px;"></span>
    <form asp-controller="Account" asp-action="Logout">
        <button class="btn btn-outline-danger my-2 my-sm-0">
            <i class="bi bi-box-arrow-in-right"></i> Log out
        </button>
    </form>*@

@section Scripts{
    <script type="text/javascript" src="js/bootstrap/bootstrap-dropdown.js">
        $(document).ready(function () {
            $('.dropdown-toggle').dropdown()
        });

    </script>

}